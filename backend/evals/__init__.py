"""Golden-set eval for the AI wizard (AI_WIZARD_PLAN "The part that actually
costs you: evals"). This package is NOT imported by the app — it is the gate
for changing `ai_text_provider`/`ai_text_model` in production."""
